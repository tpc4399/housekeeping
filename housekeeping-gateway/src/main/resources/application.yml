server:
  port: 10010
spring:
  application:
    name: housekeeping-gateway
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    registry-fetch-interval-seconds: 5
zuul:
  prefix: /api
  routes:
    admin-service: /admin/**
  add-host-header: true
  sensitive-headers: