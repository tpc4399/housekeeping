<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.housekeeping.admin.mapper.OrderDetailsMapper">
    <update id="statusAndTime">
        UPDATE order_status = #{status} and completion_date_time = #{time}
        FROM order_details
        WHERE `number` = #{number}
    </update>

    <select id="orderRetentionTime" resultType="java.lang.Integer">
        SELECT c.order_retention_time
        FROM company_details c right join employees_details e on e.company_id = c.id
        WHERE e.id = #{employeesId}
    </select>
</mapper>
