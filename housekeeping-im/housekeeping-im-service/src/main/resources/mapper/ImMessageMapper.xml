<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.housekeeping.im.mapper.ImMessageMapper">

    <select id="getAllChatIds" resultType="java.lang.Integer">
        SELECT
        DISTINCT to_id
        FROM
        im_message
    </select>
</mapper>
