<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.housekeeping.auth.mapper.HkUserMapper">

	<select id="byEmail" resultType="com.housekeeping.common.entity.HkUser">
		SELECT id as id,
			nickname as nickName,
			phone_prefix as phonePrefix,
			phone as phone,
			email as email,
			password as password,
			dept_id as deptId
		FROM `sys_user`
		WHERE email = #{email}
				and dept_id = #{deptId}
	</select>
	<select id="byPhone" resultType="com.housekeeping.common.entity.HkUser">
		SELECT id as id,
			nickname as nickName,
			phone_prefix as phonePrefix,
			phone as phone,
			email as email,
			password as password,
			dept_id as deptId
		FROM `sys_user`
		WHERE phone_prefix = #{phonePrefix}
				and phone = #{phone}
				and dept_id = #{deptId}
	</select>
</mapper>
