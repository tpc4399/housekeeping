<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.housekeeping.auth.mapper.HkUserMapper">

	<select id="byEmail" resultType="com.housekeeping.auth.utils.HkUser">
		SELECT id as id,
			nickname as nickName,
			phone as phone,
			email as email,
			password as password
		FROM `user`
		WHERE email = #{email} and del_flag = false
	</select>
	<select id="byPhone" resultType="com.housekeeping.auth.utils.HkUser">
		SELECT id as id,
			nickname as nickName,
			phone as phone,
			email as email,
			password as password
		FROM `user`
		WHERE phone = #{phone} and del_flag = false
	</select>
</mapper>
